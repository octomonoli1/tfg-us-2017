##
## You can use Velocity to define display templates for the Asset Publisher
## portlet.
##
## A set of variables have been made available for the template developers:
##
##  $assetPublisherHelper: helper class available for developers. You can use it
##                         to get the asset view url by calling
##                         getAssetViewURL($renderRequest, $renderResponse,
##                         $assetEntry)
##
##  $ddmTemplateId: the current template id
##
##  $entries: the list of asset entries that are being shown in the asset publisher
##
##  $entry: the asset entry when there's only one asset entry
##
##  $locale: the locale of the site
##
##  $renderRequest: the render request
##
##  $renderResponse: the render response
##
##  $taglibLiferay: object that provides access to Liferay's taglibs
##
##  $themeDisplay: the theme display
##
## The variable $templatesPath can be used to include another template through
## its template key, e.g. #parse ("$templatesPath/LAYOUT-PARENT").
##