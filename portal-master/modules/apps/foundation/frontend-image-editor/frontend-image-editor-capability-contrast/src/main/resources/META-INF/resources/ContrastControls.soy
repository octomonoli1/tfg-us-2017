{namespace ImageEditorContrastControls}

/**
 * ImageEditor.Controls "contrast" implementation.
 */
{deltemplate ImageEditor.Controls variant="'contrast'"}
	{call ImageEditorContrastComponent.render data="all" /}
{/deltemplate}

/**
 * Empty .render placeholder. Necessary for metal-cli AOP.
 */
{template .render}
{/template}