{namespace ImageEditorSaturationControls}

/**
 * ImageEditor.Controls "saturation" implementation.
 */
{deltemplate ImageEditor.Controls variant="'saturation'"}
	{call ImageEditorSaturationComponent.render data="all" /}
{/deltemplate}

/**
 * Empty .render placeholder. Necessary for metal-cli AOP.
 */
{template .render}
{/template}